import FormContainer from './components/FormContainer';

const App = () => {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>Formulaire avec JSON Forms</h1>
      <FormContainer />
    </div>
  );
};

export default App;
